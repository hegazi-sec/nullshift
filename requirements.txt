fastapi
uvicorn[standard]
python-dotenv
requests
openai>=1.0.0
anthropic>=0.40.0
# Optional: only needed if USE_CLAUDE_AGENT_SDK=true (home-SOC mode).
# Routes chat through the local `claude` CLI using the operator's Claude.ai
# subscription instead of an API key.
claude-agent-sdk>=0.1.0
paramiko>=3.4.0
pydantic-settings
numpy
pytest
pytest-asyncio
passlib[bcrypt]==1.7.4
python-jose==3.3.0
python-multipart
chromadb>=0.4.22
google-genai>=1.0.0
pyyaml>=6.0